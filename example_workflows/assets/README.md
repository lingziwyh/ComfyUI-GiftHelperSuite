# Example media

These files were supplied by the project owner as demo inputs for the bundled GiftHelperSuite workflows. The MP4 container metadata has been stripped without re-encoding so embedded prompts, workflows, source filenames, and local machine paths are not distributed.

The repository's MIT license covers the software code. It does not grant separate reuse rights for the example media outside these bundled workflow demonstrations.
